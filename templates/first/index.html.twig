{% extends 'base.html.twig' %}

{% block title %}
    First

{% endblock %}
{% block body %}
    {{ parent() }}

    <div>
        <a href="{{ path('todo') }}"> Go To TODO </a>
        <p>{{ url('todo') }}</p>
        <p>{{ path('todo') }}</p>
    </div>
{% endblock %}